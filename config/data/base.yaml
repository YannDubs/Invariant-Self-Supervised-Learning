defaults:
  - base_runtime

name: ${.dataset}
dataset: ???

kwargs:
  seed: ${seed}
  data_dir: ${paths.data}
  batch_size: 256
  num_workers: 8
  subset_train_size: null
  dataset_kwargs:
    seed: ${seed}
    is_normalize: True
    a_augmentations: ["data-standard"]