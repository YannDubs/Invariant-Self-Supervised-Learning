defaults:
  - base_runtime

name: ${.dataset}
dataset: ???

kwargs:
  data_dir: ${paths.data}
  batch_size: 256
  reload_dataloaders_every_n_epochs: ${trainer.reload_dataloaders_every_n_epochs}
  num_workers: 16
  dataset_kwargs:
    is_normalize: True
    a_augmentations: []