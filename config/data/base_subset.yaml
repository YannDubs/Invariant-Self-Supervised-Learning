defaults:
  - base_runtime

is_copy_repr: True
name: "data_repr_${.kwargs.subset_train_size}" # when using is_copy_repr name will be formatted
dataset: ???

kwargs:
  subset_train_size: ???
  is_test_nonsubset_train: True # test on the non subset train dataset
