defaults:
  - base_subset

name: "{name}_${.kwargs.subset_train_size}_test" # when using is_copy_repr name will be formatted

kwargs:
  subset_train_size: 0.01 # use 1% of the training set to train the downstream model
  is_test_nonsubset_train: False # test on actual test
