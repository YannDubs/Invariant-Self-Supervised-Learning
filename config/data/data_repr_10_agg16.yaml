defaults:
  - base_subset

name: "{name}_${.kwargs.subset_train_size}_agg${.kwargs.dataset_kwargs.max_k_ary_agg}" # when using `is_copy_repr` name will be formatted

kwargs:
  subset_train_size: 10
  is_test_nonsubset_train: True # test on the non subset train dataset
  dataset_kwargs:
    aux_target: agg_target
    n_agg_tasks: 16
    max_k_ary_agg: 2
