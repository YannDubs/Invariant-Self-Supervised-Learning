defaults:
  - base_runtime

is_copy_repr: True
name: "{name}_agg" # when using `is_copy_repr` name will be formatted
dataset: ???

kwargs:
  dataset_kwargs:
    aux_target: agg_target
    n_agg_tasks: 10
    max_k_ary_agg: 2