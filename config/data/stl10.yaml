defaults:
  - base

dataset: stl10

kwargs:
  val_size: 0.05 #! very little labels so that might make an impact ?
  dataset_kwargs:
    a_augmentations: [hflip,resize-crop,color,gray] #? should we also use augmentations when training the classifier ?