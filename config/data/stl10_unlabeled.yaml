defaults:
  - base
  - base_unlabeled

dataset: stl10_unlabeled

kwargs:
  val_size: 0.05 #! very little labels so that might make an impact ?
  dataset_kwargs:
    a_augmentations: [hflip,resize-crop,color,gray] #! to tune