# @package _global_
defaults:
  - cntr_500A

representor:
  name: cntr_500A_mlp

decodability:
  kwargs:
    predictor_kwargs:
      architecture: "mlp"
      hid_dim: 2048
      n_hid_layers: 2
      norm_layer: "batch"