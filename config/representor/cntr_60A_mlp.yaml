# @package _global_
defaults:
  - cntr_60A

representor:
  name: cntr_60A_mlp

decodability:
  kwargs:
    predictor_kwargs:
      architecture: "mlp"
      hid_dim: 2048
      n_hid_layers: 2
      norm_layer: "batch"