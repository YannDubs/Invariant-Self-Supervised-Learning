# @package _global_
defaults:
  - cntr

representor:
  name: cntr_coarserA

data_repr:
  kwargs:
    dataset_kwargs:
      aux_target:  "augmentation"
      a_augmentations: ["coarser"]
      train_x_augmentations: []