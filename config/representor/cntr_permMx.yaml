# @package _global_
defaults:
  - cntr

representor:
  name: cntr_permMx

data_repr:
  kwargs:
    dataset_kwargs:
      aux_target:  "augmentation"
      a_augmentations: ["perm_label"]
      train_x_augmentations: []