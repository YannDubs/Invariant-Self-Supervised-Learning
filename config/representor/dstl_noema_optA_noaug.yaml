# @package _global_
defaults:
  - base@representor
  - override /regularizer: none
  - override /decodability: distillating

representor:
  name: dstl_noema_optA_noaug

data_repr:
  kwargs:
    dataset_kwargs:
      aux_target:  "augmentation"
      a_augmentations: ["label"]
      train_x_augmentations: []

decodability:
  kwargs:
    ema_weight_prior: null
    beta_pM_unif: 2.3
    beta_HMlZ: 1.8
    out_dim: 8192
    projector_kwargs:
      architecture: "mlp"
      is_cosine: True