# @package _global_
defaults:
  - exact_250A

representor:
  name: exact_250A_mlpXXS

decodability:
  kwargs:
    predictor_kwargs:
      architecture: "mlp"
      hid_dim: 32
      n_hid_layers: 1
      norm_layer: "batch"