# @package _global_
defaults:
  - override /decodability: generative
  - override /regularizer: none

representor:
  name: std_gen

kwargs:
  decoder_kwargs:
    architecture: cnn

data_repr:
  kwargs:
    dataset_kwargs:
      aux_target:  "input"
      a_augmentations: []
      train_x_augmentations: []

encoder:
  z_shape: 128
  is_normalize_Z: False

