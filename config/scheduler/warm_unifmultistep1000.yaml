defaults:
  - base

name: warm_unifmultistep1000
modes: ["UniformMultiStepLR"] # list of scheduler to use
kwargs:
  # all kwargs
  UniformMultiStepLR:
    decay_factor: 1000
    k_steps: 3

  base:
    is_warmup_lr: True
    warmup_epochs: 0.1